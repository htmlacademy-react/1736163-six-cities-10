export enum AppRoute {
  Login = '/login',
  Favourites = '/favourites',
  Root = '/',
  Room = 'offer/:id'
}

export enum AuthorizationStatus {
  Auth='AUTH',
  NoAuth='NO_AUTH',
  Unknown='UNKNOWN'
}

export enum HousingVariants {
  room = 'Room'
}
